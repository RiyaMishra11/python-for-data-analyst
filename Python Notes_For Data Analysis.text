→← MODULE -1 PYTHON FUNDAMENTALS →← 
─────────────────────────────────────────────────────────────────
─────────────────────────────────────────────────────────────────

→← LECTURE -1 →←
───────────────────────



PYTHON :- PYTHON IS A HIGH-LEVEL , INTERPRETED, GENERAL-PURPOSE PROGRAMMING LANGUAGE WHICH IS EASY TO READ AND UNDERSTAND .

CREATOR OF PYTHON :- GUIDO VAN ROSSUM
YEAR :- 1989
RELEASED :- 1991

“PYTHON” NAMED IS INSPIRED FROM ONE COMEDY TV SHOW “MONTY PYTHON’S FLYING CIRCUS” 

WHY PYTHON IS SO POPULAR :- 
BECAUSE OF , 1) EASY SYNTAX ,  2) HUGE COMMUNITY  ,
3) THOUSANDS OF LIBRARIES , 4) CROSS PLATFORM


─────────────────────────────────────────────────────────────────

→← LECTURE -2 →←
───────────────────────

COMPUTER MEMORY :- WHEN A PROGRAM RUNS COMPUTER STORES THEIR DATA INTO RAM(MEMORY) .

VARIABLE :- VARIABLE IS A NAME GIVEN TO A MEMORY LOCATION .

NOTE :- IN PROGRAMMING “ = ” MEANS ‘ASSIGNMENT OPERATOR’ i.e. LEFT VARIABLE STORE RIGHT VALUE .
EXAMPLE :- X = 10
THAT MEANS NOT X = 10 !!!
THAT MEANS STORE 10 INSIDE X .

VARIABLE NAMING RULES :- 
VARIABLE CAN START WITH ALPHABET.
IT CAN BE START WITH UNDERSCORE. 
VARIABLE CAN’T BE START FROM NUMBER.

SPECIAL CHARACTERS IS NOT ALLOWED IN VARIABLES.
SPACE IS ALSO NOT ALLOWED IN VARIABLE .
VARIABLE IS CASE SENSITIVE.

IDENTIFIRES :- IDENTIFIERS ARE THE NAMES USED TO IDENTIFY VARIABLES , FUNCTIONS , CLASSES ETC.

KEYWORDS :- KEYWORDS ARE RESERVED WORDS WE CAN’T MAKE THEM VARIABLES .
EXAMPLE :- IF , ELSE , ELSE IF , FOR , WHILE ETC .

LITERALS : - LITERAL MEANS ACTUAL VALUES .
EX - AGE = 20 | VARIABLE → AGE | LITERAL → 20

ISINSTANCE() :- THIS FUNCTION IS USED TO CHECK THAT THE GIVEN OBJECT’S TYPE IS PARTICULAR OR NOT .
EX- AGE = 20
PRINT(ISINSTANCE(AGE,INT))
OUTPUT :- TRUE


─────────────────────────────────────────────────────────────────

→← LECTURE -3 →← 
───────────────────────

‘SEP’ PARAMETER -: MEANS SEPARATOR PARAMETER { “ ” }
EX - PRINT (“APPLE”, “BANANA” , “MANGO” , SEP= “-” )
OUTPUT - APPLE-BANANA-MANGO

‘END’ PARAMETER -: 
DEFAULT VALUE OF END IS → END=”\N”
BUT ,
WE CAN CHANGE IT , LIKE 
PRINT(“HELLO” , END= “ ”)
PRINT(“WORLD”)
OUTPUT : HELLO WORLD

ESCAPE CHARACTERS :- 
NEW LINE →
PRINT(“HELLO\nWORLD”)
OUTPUT :- HELL0 WORLD
TAB →
PRINT(“NAME\tAGE”)
OUTPUT :- NAME   AGE
DOUBLE QUOTE →
PRINT(“MY NAME IS \ “PRASHANT\””)
OUTPUT :- MY NAME IS “PRASHANT”

INPUT :- 
input() ALWAYS RETURNS STRING (str ) 

TYPECASTING -: CONVERSION FROM ONE DATA TYPE TO ANOTHER DATA TYPE.
EXAMPLE OF TYPECASTING – 
PRICE = FLOAT(INPUT(“PRICE : ”))

TWO METHODS OF TYPE CONVERSION 
IMPLICIT TYPE CONVERSION :- MEANS AUTOMATIC CONVERSION .
EXPLICIT TYPE CONVERSION :- MEANS MANUAL CONVERSION . 


─────────────────────────────────────────────────────────────────

→← LECTURE- 4 →← OPERATORS →←
───────────────────────────────

OPERATORS IS A SYMBOL WHICH PERFORMS OPERATIONS ON VALUES(OPERANDS) .

TYPES OF OPERATORS :- 
ARITHMETIC → (+,-,*,/,//,%,**)
ASSIGNMENT → (=,+=,-=,*=,/=)
COMPARISON → (==,!=,>,<,>=,<=)
LOGICAL → (AND , OR , NOT)
MEMBERSHIP → (IN , NOT IN)
IDENTITY → (IS , IS NOT)
BITWISE → (&,|,^,~,<<,>>)

OPERATOR PRECEDENCE →: 
EASY MEMORY TRICK (HIGHEST → LOWEST)
() PARENTHESES
** 
*,/,//,%
+,-
COMPARISON (<,>,==,etc)
NOT
AND
OR




─────────────────────────────────────────────────────────────────

→← LECTURE-5 →← STRINGS
───────────────────────────────

STRINGS IS A SEQUENCE OF CHARACTERS ENCLOSED INSIDE QUOTES .
EX : NAME = “PRASHANT”

NOTE : IN PYTHON CHARACTER IS ALSO A STRING .

THERE ARE MAINLY FOUR METHODS TO CREATE STRINGS :-
SINGLE QUOTES - ‘PRASHANT’
DOUBLE QUOTES - “TIWARI”
TRIPLE SINGLE QUOTES - ‘’’PRASHANT’’’
TRIPLE DOUBLE QUOTES - “””RIYA
TIWARI
B.TECH “””
(THIS IS USED WHEN WE HAVE TO WRITE MULTI-LINE STRING)

BASIC BUILT-IN STRING FUNCTIONS : 
split() :- 
SPLIT FUNCTION IS USED TO CONVERT STRING INTO LIST .

join() :- 
JOIN FUNCTION IS USED TO CONVERT LIST INTO STRING.

isalpha() :- 
CHECK THAT THE GIVEN STRING IS CONSIST ONLY ALPHABET OR NOT .

isdigit() :- 
CHECK THAT THE GIVEN INPUT IS CONSIDER ONLY DIGITS .

isalnum() :- 
CHECK THAT THE GIVEN STRING IS CONSIDER LETTERS + NUMBERS .
NOTE :- IF SPACE IS BETWEEN LETTERS AND NUMBERS IT WILL RETURN FALSE .

islower() :- 
CHECK THAT THE GIVEN STRING IS CONSIDER ONLY LOWERCASE LETTERS .

isuppeer() :-  CHECK THAT THE GIVEN STRING IS CONSIDER ONLY UPPERCASE LETTERS .

isspace() :- 
GIVEN INPUT CONSIDERS SPACE OR NOT .

istitle() :- 
CHECK THAT THE GIVEN STRING IS IN THE FORM OF TITLE. 

casefold() :- 
ADVANCE VERSION OF lower() .
IT IS BETTER FOR UNICODE TEXT.

STRING FORMATTING :- 

METHOD 1 (OLD) :- 
EX :- name = “Prashant”
print(“Hello” , name)

METHOD 2 (.format()) :- 
EX :- name = “Prashant”
Age = 21
print(“My name is {} and I am {} years old .”.format(name,Age))

METHOD 3 (f-string) :- 
EX :- name = “Prashant”
         Age = 21
         print(f “My name is {name} and I am {age} year old .”)



─────────────────────────────────────────────────────────────────

→← LIST { MASTERCLASS - 1 } →←
───────────────────────────────

LIST IS AN ORDERED , MUTABLE COLLECTION OF MULTIPLE ITEMS .

LIST vs STRING :- 
LIST MUTABLE WHILE STRING IS IMMUTABLE
STRING IS COLLECTION OF CHARACTERS WHILE LIST IS COLLECTION OF ANY OBJECTS
STRING WROTE INSIDE THE QUOTES “” WHILE LIST IS WRITTEN INSIDE SQUARE BRACKET [] 


→← LIST METHODS →← 
APPEND () :- IT ADD AN ELEMENT IN THE LIST AT END .
EX:- fruits = ["Apple", "Banana"]
fruits.append("Orange")
print(fruits)
Output:
['Apple', 'Banana', 'Orange']


EXTEND () :- IT ADDS MULTIPLE ELEMENTS IN LIST AT END .
EX:- fruits = ["Apple", "Banana"]
fruits.extend(["Orange", "Mango"])
print(fruits)
Output : 
['Apple', 'Banana', 'Orange', 'Mango']

INSERT () :- INSERT DATA AT ANY POSITION.
SYNTAX : - list.insert(index,value)
EX:- fruits = [“Apple”, “Orange”]
fruits.insert(1, “Banana”)
Print (fruits)
Output :- 
['Apple', 'Banana', 'Orange']

REMOVE () :- IT DELETE VALUES .
EX:- fruits = ["Apple","Banana","Orange"]
fruits.remove("Banana")
print(fruits)
OUTPUT :- 
['Apple', 'Orange'] 

POP () :- IT DELETE ON THE BASIS OF INDEX.
EX:- 
numbers = [10,20,30]
numbers.pop(1)
print(numbers)
OUTPUT :- 
[10,30]


🎯 remove() vs pop()
REMOVE() FUNCTION REMOVE FROM VALUE WHILE POP() FUNCTION REMOVE FROM INDEX 
REMOVE() DOESN’T RETURN ANY VALUE WHILE POP() RETURN REMOVED ITEM.

CLEAR() :- IT WILL DELETE EVERYTHING. 
EX:- 
nums = [10,20,30]
nums.clear()
print(nums)
OUTPUT :- 

[]

COPY () :- WHEN WE ARE COPYING ANYTHING 
EX:-
list1 = [10,20,30]
list2 = list1.copy()
— — —
list2[0] = 100
OUTPUT :- 
list1 = [10,20,30]
list2 = [100,20,30]



##→ ASSIGNMENT (=) vs COPY()
👍ASSIGNMENT 
list2 = list1
list1 → [10,20,30] ← list 2   (Same Object)

👍COPY ()
list2 = list1.copy()
list1 → [10,20,30]
list2 → [10,20,30]
   CREATES DIFFERENT OBJECTS.

SORT() :- SORTING IN THE GIVEN FORMAT.
Ascending Sort : 
EX:- nums = [50,20,90,10]
nums.sort()
print(nums)

OUTPUT :- 
[10,20,50,90]



Descending Sort : 
nums.sort(reverse=True)
OUTPUT:- 
[90,50,20,10]



String Sorting : 
EX:- fruits = ["Mango","Apple","Orange"]
fruits.sort()
print(fruits)

OUTPUT :- 
['Apple','Mango','Orange']
REVERSE() :- IT JUST REVERSE THE STRING.
EX: nums=[10,20,30]
nums.reverse()
print(nums)
OUTPUT :- 
[30,20,10]


NOTE:- nums[::-1] it gives new reversed view while reverse() just reversed the original list.

COUNT() :- IT JUST COUNT THE VALUES.
EX: nums=[10,20,20,30]
print(nums.count(20))
OUTPUT :- 
2

INDEX() :- THIS FUNCTION IS USED TO FIND THE INDEX OF ANY ELEMENT PRESENT IN THE LIST.
EX:- fruits=["Apple","Banana","Orange"]
print(fruits.index("Banana"))
OUTPUT :- 
1


─────────────────────────────────────────────────────────────────────────────────────

→← LECTURE 10 →←Advanced List Operations & Built-in Functions →←
─────────────────────────────────────────────────────────────────

1 LEN() :- IT FIND THE NUMBER OF ELEMENTS PRESENT INTO THE LIST .
SYNTAX:- PRINT(LEN(FRUITS))

2 SUM() :- IT WORKS ONLY ON THAT LIST WHICH CONSIST ONLY NUMBERS.
SYNTAX:- PRINT(SUM(MARKS))

3 MIN() :- IT PRINTS THE MINIMUM VALUE PRESENT INTO TO LIST.
SYNTAX:- PRINT(MIN(NUMS))

4 MAX() :- IT PRINTS THE MAXIMUM VALUE PRESENT IN THE GIVEN LIST .
SYNTAX :- PRINT(MAX(NUMS))

5 SORTED() :- IT SORT THE GIVEN LIST . BUT IT DOES NOT CHANGE THE ORIGINAL LIST LIKE SORT().
SYNTAX :- NEW_LIST = SORTED(NUMS)

6 LIST CONCATENATION(+) :- IT ADDS TWO LISTS/.
SYNTAX:- PRINT(LIST1 + LIST2)

7 LIST REPETITION(*) :- IT PRINTS THE LIST TO THE NUMBER OF REPETITION WHICH IS GIVEN.
SYNTAX:- PRINT(NUMS * 3)

8 MEMBERSHIP :- IT IS AN OPERATOR WHICH CHECKS THAT THE ASKED VALUE IS PRESENT INTO THE LIST OR NOT.
TWO TYPES :- IN & NOT IN



─────────────────────────────────────────────────────────────────────────────────────
→← Lecture 11 Tuples (Immutable Collections) →←
───────────────────────────────────────────────────

Tuples :- Tuple is like List , it is also a collection . We can’t change elements once created . This property called “Immutable”.
Example :- 
fruits = (“Apple”, “Banana” , “Orange”)
print(fruits)
Output :- 
(‘Apple’, ‘Banana’, ‘Orange’)

List vs Tuple 
1 List is mutable while tuple is immutable.
2 List uses [] but tuple uses ().
3 List is slow while Tuple is faster.
4 List need much memory but Tuple need less memory.

How to create single element tuple : 
num = (10) → wrong , because it is ‘int’ type of data
num = (10,) → correct , now it is ‘tuple’ type of data

→ Indexing : Same as List.
→ Slicing : Same as List.

→ Packing : 
student = (“Prashant”,20, “BCA”)
: Three values pack into a tuple.


→ Unpacking : 
name,age,course = student
print(name)
print(age)
print(course)

Tuple have only two methods , 1. count() & 2. index()

→ Membership operators : Same as List. (in , not in)
→ Built-In Functions : Same as List (len , min , max , sum)

Nested Tuple : 
Ex: matrix = (
(1,2),
(3,4)
)

Access : 
print(matrix[1][0])
Output : 
3

a)  Tuple Conversion :-  
    List → Tuple
    Ex: 
    nums = [10,20,30]
t = tuple(nums)
print(t)
Output : 
(10,20,30)

b) Tuple → List
 Ex:
 t = (10,20,30)

nums = list(t)
print (nums)
Output: 
[10,20,30]

─────────────────────────────────────────────────────────────────────────────────────

→← Lecture 12 →← Sets in Python →← 
───────────────────────────────────────────────────

Sets :- Set is a collection which is : 
1. Unordered 
2. Which doesn’t allow duplicate value
3. It is mutable
4. It does not support indexing
5. It is made using curly brackets { }

Example : 
numbers = {10,20,30,40,50}
print(numbers)

→ Duplicate Values 
numbers = {10,20,30,20,30}
print(numbers)
Output :- 
{10,20,30} → Python automatically removes the duplicate values.

Example : 
numbers = {10,20,30,40,50}
print(numbers)

Notes :- 
a) List → duplicates allowed
b) Tuple → duplicates allowed
c) Set → duplicates not allowed

my_set = {} → it is not an empty set , actually it is an empty dictionary.
Correct form of empty set : 
my_set = set()

→ Indexing
Set does not perform indexing operations because it is an unordered collection.

→ add() 
To add one element into the set use “add()”
Example: 
fruits = {“Apple” , “Banana”}
fruits.add(“Orange”)
print(fruits)

→ update()
To add multiple elements into the set use “update()”
Example : 
fruits = {“Apple” , “Banana”}
fruits.update([“Orange”, “Mango” , “Guava”])
print(fruits)

→ remove()
To remove any element from set use “remove()”
Example : 
fruits = {“Apple” , “Banana” , “Orange”}
fruits.remove(“Banana”)
print(fruits)

Note:- If element does not exist remove will through error.

→ discard()
This is also used to remove elements from a set.
The discard function does not throw an error if the element is not present into the set like remove().

→ pop()
The pop function removes one element from the set.
Example : 
numbers = {10,20,30,40,50}
numbers.pop()
print(numbers)

→ clear() 
It will clear the whole set.
Example:
numbers = {10,20,30,40}
numbers.clear()
print(numbers)

→ Membership Operator : Same as list and tuple (in&not in)

→ Set Operations 
Suppose : 
A = {1,2,3,4}
B = {3,4,5,6}

👍Union : Return unique elements from both sets.
Example : print(A | B)

👍Union : Return unique elements from both sets.
Example : print(A | B)
Output : {1,2,3,4,5,6}
Using Method : 
print(A.union(B))

👍Intersection : Return common elements from both sets.
Example : print(A & B)
Output : {3 , 4}
Using Method : 
print(A.union(B))

👍Difference : Return elements which are present into A but don't 
present into B.
Example : print(A - B)
Output : {1,2}
Reverse : print(B - A)
Reverse : print(B - A)

👍Symmetric Difference : Return those elements which are present 
in set A or set B , but not common elements.
Example: print(A ^ B)
Output : {1,2,5,6}
Using Method : 
print(A.symmetirc_difference(B))

→ Subset 
If elements of set A are present into the set B then it is called as A is the subset of B.
Example : 
A = {1,2}
B = {1,2,3,4,5,6}
print(A.issubset(B))
Output : 
True


